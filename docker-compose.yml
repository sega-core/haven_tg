services:
  backend:
    build: .
    container_name: haven_tg
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - haven_network
    command: ["node", "dist/server.js"]

networks:
  haven_network:
    driver: bridge